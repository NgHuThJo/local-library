<h1><%= title %></h1>
<% if(book_list.length) { %>
<ul>
  <% book_list.forEach((book) => { %>
  <li>
    <a href="<%= book.url %>"><%- book.title %></a>
    <p><%= book.author.name %></p>
  </li>
  <% }) %>
</ul>
<% } else { %>
<p>There are no books.</p>
<% } %>
